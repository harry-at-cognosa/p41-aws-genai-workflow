[pytest]
testpaths = tests
markers =
    integration: hits the deployed AWS stack (requires API_BASE_URL and API_KEY env vars)
# Default: skip integration tests unless explicitly requested with `-m integration`.
addopts = -m "not integration"
